# Mehwish Sultana — Commit Plan
**Role:** Database & Config · Customer Dashboard · Admin Dashboard · Reports · Testing & Deployment  
**Period:** 27 May – 1 June  
**Total Commits:** 10

---

## Commit Table

| # | Date | Time | Files to Commit | Commit Message | What You Learnt | Major Logic Used |
|---|------|------|----------------|----------------|-----------------|-----------------|
| 1 | 27 May | 12:00 PM | `src/main/resources/application.properties` | `chore: configure MySQL datasource, JPA, Thymeleaf, and server properties` | `spring.jpa.hibernate.ddl-auto=update` makes Hibernate auto-create or alter tables to match entities on startup — safe for development, never use `create-drop` in production as it wipes data; `spring.thymeleaf.cache=false` is essential in dev so template changes reload without restarting | `spring.datasource.url` connection string uses `useSSL=false&allowPublicKeyRetrieval=true` flags required by MySQL 8+/9 to avoid handshake errors; `serverTimezone=UTC` prevents timezone mismatch warnings |
| 2 | 27 May | 04:00 PM | `DataInitializer.java` | `feat: add DataInitializer to seed default users and sample products on startup` | `CommandLineRunner` runs after the full Spring context is ready — the correct place for data seeding because all beans (`UserService`, `ProductRepository`) are fully injected by then; a guard check (`existsByUsername`, `productRepository.count() > 0`) prevents duplicate inserts on every restart | `BCryptPasswordEncoder` called through `UserService.registerUser()` ensures passwords are hashed the same way as normal registration — using raw SQL inserts with hardcoded hashes is unreliable because the hash must exactly match the encoder's output |
| 3 | 28 May | 09:00 AM | `controller/CustomerController.java`, `templates/customer/dashboard.html` | `feat: add customer dashboard with cart count, order stats, and quick navigation tiles` | `stream().limit(5).toList()` efficiently takes only the 5 most recent orders for the dashboard without loading all orders into memory; dashboard stats (cart count, order count) passed as separate model attributes rather than embedded in objects keeps the template logic simple | Bootstrap card grid used for navigation tiles — each tile is an `<a>` tag styled as a card with hover lift effect via CSS `.dashboard-tile`; `th:text="${cartCount}"` on the cart nav badge updates dynamically from session data |
| 4 | 28 May | 05:00 PM | `controller/CustomerController.java` (profile routes added), `templates/customer/profile.html` | `feat: add customer profile view and update with password change support` | Updating a profile requires checking uniqueness only for changed values — if the user keeps the same username, `existsByUsername` would wrongly flag it as taken; the check must be `!user.getUsername().equals(newUsername) && existsByUsername(newUsername)` | `session.setAttribute("loggedInUser", updatedUser)` must be called after saving — otherwise the session still holds the old object and the navbar shows the old name; BCrypt re-encoding only triggered when `newPassword` field is non-blank |
| 5 | 29 May | 11:00 AM | `service/DashboardService.java`, `controller/AdminController.java`, `templates/admin/dashboard.html` | `feat: add admin dashboard with total products, orders, customers, today's sales, and low stock alerts` | `LocalDateTime.now().withHour(0).withMinute(0).withSecond(0)` gets start of today for filtering today's sales; `getLowStockProducts()` uses a JPQL query with `WHERE p.quantityOnHand < 5` — the threshold is a business rule that should be a named constant | Admin dashboard is a read-only aggregation view — all data fetched via `DashboardService` which calls multiple repositories; no writes happen on the dashboard, so no `@Transactional` needed |
| 6 | 30 May | 09:30 AM | `controller/ReportController.java`, `templates/admin/reports/dashboard.html`, `templates/admin/reports/sales.html` | `feat: add reports hub and sales report with date range filter and revenue summary` | `@DateTimeFormat(iso = DateTimeFormat.ISO.DATE)` on a `LocalDate` parameter automatically parses `?from=2026-05-01` URL params into Java date objects; `BigDecimal.divide()` requires a `RoundingMode` argument — without it, dividing non-terminating decimals throws `ArithmeticException` | Average order value calculated as `totalRevenue.divide(BigDecimal.valueOf(orders.size()), 2, RoundingMode.HALF_UP)`; `ReportController` uses an inner `record CustomerSummary(User user, int orderCount)` — Java records are ideal for read-only data transfer without boilerplate getters |
| 7 | 30 May | 02:30 PM | `templates/admin/reports/inventory.html`, `templates/admin/reports/customers.html` | `feat: add inventory report with stock status badges and customer activity report` | Thymeleaf chained `th:if` blocks (Out of Stock → Low Stock → In Stock) work like if/else-if — only the first matching condition renders; this avoids a complex `th:switch` for simple badge logic | Customer report builds a `List<CustomerSummary>` by joining all CUSTOMER-role users with their order counts via Java stream mapping — avoids a complex JOIN query while keeping the logic readable |
| 8 | 31 May | 09:00 AM | `templates/admin/reports/dashboard.html` (links fix), `start.sh` | `feat: add automated start script with MySQL install, service start, and DB creation` | Polling with `mysqladmin ping` is more reliable than a fixed `sleep` because MySQL startup time varies — especially on first run after install when it initialises the data directory | `start.sh` uses a `.setup_done` flag file to run first-time password reset only once; `sed -i ''` (macOS BSD sed) updates `application.properties` in-place — the empty string after `-i` is required on macOS unlike Linux |
| 9 | 31 May | 03:00 PM | `templates/admin/feedback.html` | `feat: add admin feedback report page with rating colour coding` | Thymeleaf `th:classappend` conditionally appends a CSS class without replacing existing classes — used here to colour ratings green (≥4), yellow (3), red (<3) using Bootstrap text utilities | `#temporals.format(fb.feedbackDate, 'dd MMM yyyy')` is Thymeleaf's built-in date formatter — no need for a custom formatter bean for standard display patterns |
| 10 | 1 Jun | 12:00 PM | `ISSUES.md`, `static/favicon.svg`, all HTML templates (favicon link added) | `docs: add setup issues log, favicon, and fix all template head sections` | Every browser requests `/favicon.ico` automatically on page load — if the server returns 500 instead of 404 it pollutes the error logs; serving an SVG favicon via `<link rel="icon" type="image/svg+xml">` is the modern approach and avoids needing a `.ico` converter | `favicon.svg` uses a simple inline SVG with a blue rounded rectangle and "e" text — no external tools needed; `sed -i ''` used to batch-add the favicon `<link>` tag to all 24 templates in one command |

---

## Summary

- **Day 1 (27 May):** application.properties, DataInitializer seed data
- **Day 2 (28 May):** Customer dashboard, customer profile
- **Day 3 (29 May):** Admin dashboard, DashboardService
- **Day 4 (30 May):** Reports module — sales, inventory, customers
- **Day 5 (31 May):** Admin feedback page, start.sh automation
- **Day 6 (1 Jun):** Documentation, favicon, final template fixes
