# HeenuReet — Commit Plan
**Role:** Login & Registration · Product Search & Browse · Ordering · Feedback · Final Integration  
**Period:** 27 May – 1 June  
**Total Commits:** 12

---

## Commit Table

| # | Date | Time | Files to Commit | Commit Message | What You Learnt | Major Logic Used |
|---|------|------|----------------|----------------|-----------------|-----------------|
| 1 | 27 May | 10:00 AM | `pom.xml`, `OnlineShoppingSystemApplication.java` | `chore: initialise Spring Boot project with core dependencies` | How Spring Boot auto-configures itself via `@SpringBootApplication` and how `BCryptPasswordEncoder` is registered as a `@Bean` for app-wide injection | Spring Boot starter parent manages all dependency versions; `@Bean` in main class makes encoder available to all services via `@Autowired` |
| 2 | 27 May | 02:00 PM | `entity/UserRole.java`, `entity/User.java` | `feat: add User entity with CUSTOMER and ADMIN roles` | JPA `@Entity` maps a Java class directly to a DB table; `@Enumerated(EnumType.STRING)` stores role as readable text instead of an integer | `@GeneratedValue(strategy = GenerationType.IDENTITY)` lets MySQL auto-increment IDs; `@Column(unique=true)` enforces uniqueness at DB level |
| 3 | 27 May | 05:00 PM | `repository/UserRepository.java`, `service/UserService.java` | `feat: add UserRepository and UserService with BCrypt password encoding` | Spring Data JPA generates SQL from method names like `findByUsername` — no manual queries needed; BCrypt one-way hashing means you can only verify, never reverse | `BCryptPasswordEncoder.encode()` on register; `passwordEncoder.matches(raw, hashed)` on login — the hash includes a random salt so same password gives different hashes each time |
| 4 | 28 May | 09:30 AM | `controller/AuthController.java` | `feat: add AuthController for login, register, and logout` | `HttpSession` stores the logged-in user object server-side; `RedirectAttributes.addFlashAttribute()` survives a redirect whereas `Model` does not | POST-Redirect-GET pattern prevents form resubmission on browser refresh; session key `loggedInUser` used throughout all controllers for auth checks |
| 5 | 28 May | 01:00 PM | `templates/login.html`, `templates/register.html` | `feat: add Thymeleaf login and registration pages` | Thymeleaf `th:action="@{/login}"` builds URL-context-aware links; `th:if="${error}"` conditionally renders error banners without JavaScript | Bootstrap 5 input-group wraps icon + input together cleanly; `autocomplete="current-password"` and `autocomplete="new-password"` are required by browsers for password managers |
| 6 | 28 May | 04:30 PM | `repository/ProductRepository.java`, `service/ProductService.java`, `controller/ProductController.java` | `feat: add product search and category browse for customers` | Spring Data JPA derived query `findByNameContainingIgnoreCase` performs a `LIKE %keyword%` search automatically; combining name + category filter required a separate derived method | `@RequestParam(required=false)` allows optional query params so `/products` and `/products?search=phone&category=ELECTRONICS` both hit the same controller method |
| 7 | 29 May | 10:00 AM | `templates/products/list.html`, `templates/products/detail.html` | `feat: add product listing and detail pages with search and category filter` | `th:each` iterates a model list into table rows or cards; `th:href="@{/products/category/{cat}(cat=${cat})}"` builds dynamic URLs with path variables | Category dropdown populated from `Category.values()` passed via model; Bootstrap card grid (`col-md-3`) gives responsive 4-column layout on desktop |
| 8 | 29 May | 03:00 PM | `entity/OrderStatus.java`, `entity/Order.java`, `entity/OrderItem.java`, `repository/OrderRepository.java`, `repository/OrderItemRepository.java`, `service/OrderService.java` | `feat: add Order entities and service with cart-to-order placement and cancellation` | `@OneToMany(cascade = CascadeType.ALL)` means saving an Order also saves all its OrderItems automatically; cancellation must restore product stock — `@Transactional` ensures both the status update and inventory restore succeed together or both roll back | Business rule: only `PENDING` or `CONFIRMED` orders can be cancelled (`canCancelOrder()` method); `createOrderFromCart()` copies price at time of order so later product price changes don't affect past orders |
| 9 | 30 May | 09:00 AM | `controller/OrderController.java`, `templates/orders/confirmation.html`, `templates/orders/history.html`, `templates/orders/details.html`, `templates/orders/cancel-confirm.html` | `feat: add order placement, history, details, and cancel confirmation pages` | A dedicated cancel-confirm page is better UX than a JS `confirm()` dialog because it shows full order details before the user decides; `GET /orders/{id}/cancel` shows the page, `POST /orders/{id}/cancel` executes it | Ownership check: `order.getUser().getId().equals(user.getId())` prevents customers viewing each other's orders; admin bypasses this check via role check |
| 10 | 30 May | 02:00 PM | `entity/Feedback.java`, `repository/FeedbackRepository.java`, `service/FeedbackService.java`, `controller/FeedbackController.java` | `feat: add customer feedback submission and admin feedback viewer` | `@ManyToOne(optional=true)` on the product field allows general feedback (not tied to a product); `findAllByOrderByFeedbackDateDesc()` is a derived JPA method for sorted results | Rating validated at 1–5 range using `@Min(1) @Max(5)` bean validation annotations; `@PrePersist` sets `feedbackDate` automatically on save |
| 11 | 31 May | 10:00 AM | `controller/SessionInterceptor.java`, `controller/GlobalExceptionHandler.java`, `templates/fragments/navbar.html`, `templates/error.html`, `templates/error/404.html` | `feat: add session interceptor, global error handler, and shared navbar fragment` | `HandlerInterceptor.preHandle()` runs before every controller — returning `false` blocks the request; implementing `WebMvcConfigurer` in the same class lets it self-register with `addInterceptors()` | Thymeleaf `th:replace="~{fragments/navbar :: navbar}"` injects the navbar fragment; role-based menu visibility uses `th:if="${session.loggedInUser?.role?.name() == 'ADMIN'}"` |
| 12 | 1 Jun | 11:00 AM | `DataInitializer.java`, `templates/login.html` (autocomplete fix), `static/favicon.svg` | `fix: seed users via DataInitializer, fix autocomplete attributes, add favicon` | `CommandLineRunner.run()` executes after the full Spring context is ready — the right place to seed data because all beans are available; hardcoded BCrypt hashes in SQL are unreliable because they must exactly match the encoder's output | `existsByUsername()` guard prevents duplicate inserts on every restart; `BCryptPasswordEncoder` used directly in `DataInitializer` ensures the hash is always valid for `password123` |

---

## Summary

- **Day 1 (27 May):** Project skeleton, User entity, UserRepository, UserService
- **Day 2 (28 May):** AuthController, Login/Register pages, Product search
- **Day 3 (29 May):** Product pages, Order entities, OrderService
- **Day 4 (30 May):** Order pages, Feedback module
- **Day 5 (31 May):** Session interceptor, error handling, navbar
- **Day 6 (1 Jun):** Final fixes — DataInitializer, favicon, autocomplete
