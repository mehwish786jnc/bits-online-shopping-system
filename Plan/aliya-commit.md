# Aliya — Commit Plan
**Role:** Product Management (Admin) · Shopping Cart · Order Cancellation · Sales Analysis · UI/UX & Validation  
**Period:** 27 May – 1 June  
**Total Commits:** 11

---

## Commit Table

| # | Date | Time | Files to Commit | Commit Message | What You Learnt | Major Logic Used |
|---|------|------|----------------|----------------|-----------------|-----------------|
| 1 | 27 May | 11:00 AM | `entity/Category.java`, `entity/Product.java` | `feat: add Product entity and Category enum with display names` | Java enums can carry data — `Category` holds a `displayName` field so `ELECTRONICS` renders as `"Electronics"` in the UI without any extra mapping; `@PreUpdate` annotation auto-sets `updatedDate` whenever the entity is saved | `@Column(precision=10, scale=2)` on `BigDecimal price` ensures MySQL stores it as `DECIMAL(10,2)` — correct for money; `@NotBlank` and `@Min` bean validation annotations catch bad input before it hits the DB |
| 2 | 27 May | 03:30 PM | `repository/ProductRepository.java`, `service/ProductService.java` | `feat: add ProductRepository with search/filter and ProductService CRUD` | Spring Data JPA can combine multiple filters in one derived method: `findByNameContainingIgnoreCaseAndCategory()` generates a `WHERE name LIKE ? AND category = ?` query automatically | `productRepository.findById(id).orElseThrow()` is cleaner than null-checking; `existsById()` used before delete to return a meaningful error instead of a silent no-op |
| 3 | 27 May | 06:00 PM | `controller/AdminProductController.java`, `templates/admin/products/list.html`, `templates/admin/products/add.html`, `templates/admin/products/edit.html` | `feat: add admin product management CRUD with list, add, and edit pages` | Thymeleaf `th:object="${product}"` binds a form to a model object; `th:field="*{name}"` then binds each input to a field — Spring MVC maps form POST data back to the object automatically | `@RequestMapping("/admin/products")` on the class + `@GetMapping("/add")` on the method composes to `/admin/products/add`; `POST /admin/products/delete/{id}` used instead of DELETE because HTML forms only support GET and POST |
| 4 | 28 May | 10:30 AM | `entity/Cart.java`, `entity/CartItem.java` | `feat: add Cart and CartItem entities with auto-calculated subtotal` | `@OneToMany(cascade = CascadeType.ALL, orphanRemoval = true)` on Cart means deleting a CartItem from the list and saving Cart automatically removes it from the DB — no separate delete call needed | `@PrePersist` and `@PreUpdate` on CartItem auto-calculate `subtotal = price × quantity`; `getTotal()` on Cart sums all item subtotals using Java streams: `items.stream().map(CartItem::getSubtotal).reduce(BigDecimal.ZERO, BigDecimal::add)` |
| 5 | 28 May | 03:00 PM | `repository/CartRepository.java`, `repository/CartItemRepository.java`, `service/CartService.java` | `feat: add CartService with add, remove, update quantity, and cart total logic` | `getOrCreateCart()` is a key pattern — it either fetches the existing cart for the user or creates a new one, so controllers never deal with the null case; stock validation must happen in the service before adding to cart | `findByCartIdAndProductId()` in CartItemRepository checks if a product is already in the cart — if yes, increment quantity; if no, create a new CartItem. This prevents duplicate rows. |
| 6 | 29 May | 09:00 AM | `controller/CartController.java`, `templates/cart/cart.html` | `feat: add cart controller and cart view page with quantity update and remove` | HTML forms can only POST to one action — quantity update and item remove need separate forms or hidden inputs to distinguish the operation; Bootstrap table with inline forms gives a clean cart layout | Cart total displayed using Thymeleaf: `th:text="${cart.total}"` calls the `getTotal()` method on the Cart entity directly from the template — no need to pass it separately in the model |
| 7 | 29 May | 02:00 PM | `service/OrderService.java` (cancel + restore inventory methods) | `feat: add order cancellation with inventory restore in OrderService` | `@Transactional` on `cancelOrder()` is critical — if stock restore succeeds but status update fails (or vice versa), the whole operation rolls back; without it you'd have inconsistent state | Business rule enforced in `canCancelOrder()`: only `PENDING` or `CONFIRMED` orders are cancellable; cancelled order loops through `orderItems` and calls `product.setQuantityOnHand(qoh + item.getQuantity())` for each |
| 8 | 30 May | 10:00 AM | `service/SalesAnalysisService.java`, `repository/OrderRepository.java` (JPQL queries) | `feat: add SalesAnalysisService with weekly, monthly, quarterly, yearly sales and fast/slow moving products` | JPQL `@Query` lets you write object-oriented queries against entity classes instead of table names: `SELECT o FROM Order o WHERE o.orderDate BETWEEN :start AND :end`; `GROUP BY` with `SUM` and `COUNT` aggregations give sales totals and product movement data | Fast-moving products ranked by total units sold via `SELECT oi.product.name, SUM(oi.quantity) FROM OrderItem oi GROUP BY oi.product ORDER BY SUM(oi.quantity) DESC`; slow-moving is the reverse order — both use the same query with different sort direction |
| 9 | 30 May | 03:30 PM | `controller/SalesController.java`, `templates/admin/sales/dashboard.html` | `feat: add sales dashboard with period tabs and fast/slow moving product tables` | Admin-only access enforced by checking `user.getRole() == UserRole.ADMIN` at the top of every controller method — redirects to customer dashboard if not admin | Period summary cards (weekly/monthly/quarterly/yearly) each link to a detail route; fast/slow moving tables use `th:each` over a `List<Object[]>` where each row is `[productName, category, unitsSold]` |
| 10 | 31 May | 11:00 AM | `static/js/validation.js` | `feat: add client-side form validation for required fields, email, password strength, and numeric inputs` | `addEventListener('blur')` fires when a user leaves a field — better UX than validating only on submit because errors appear immediately; `classList.add('is-invalid')` + Bootstrap's `invalid-feedback` class shows red borders and messages natively | `document.querySelectorAll('[required]')` selects all required fields generically so the validator works across all forms without per-form setup; double-submit prevention by disabling the submit button with a 10ms `setTimeout` (allows the form to actually submit first) |
| 11 | 1 Jun | 10:00 AM | `static/css/main.css`, `templates/admin/products/list.html` (search filter fix), `templates/admin/products/add.html`, `templates/admin/products/edit.html` | `fix: polish admin product pages, add CSS custom properties, fix category dropdown` | CSS custom properties (`--primary`, `--accent`) defined on `:root` make colour changes global — change once, applies everywhere; `.product-card:hover` transform creates a lift effect with `translateY(-3px)` | Bootstrap 5 utility classes handle 80% of layout; custom CSS only needed for project-specific components like `.dashboard-tile`, `.product-card`, and `.auth-container`; `box-shadow` on cards uses `rgba` for soft depth without harsh borders |

---

## Summary

- **Day 1 (27 May):** Product entity, Category enum, ProductService, Admin product CRUD
- **Day 2 (28 May):** Cart entities, CartService
- **Day 3 (29 May):** CartController, cart page, order cancellation logic
- **Day 4 (30 May):** SalesAnalysisService, SalesController, sales dashboard
- **Day 5 (31 May):** Client-side validation JS
- **Day 6 (1 Jun):** CSS polish and admin page fixes
